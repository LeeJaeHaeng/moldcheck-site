# MoldCheck 판매 사이트 배포 안내

## 무엇이 만들어졌나
`MoldCheck_site` 폴더 = 그대로 공개되는 정적 웹사이트입니다.

| 파일 | 내용 |
|---|---|
| index.html | 판매 랜딩 페이지 (체험 데모·측정된 정확도·기능·26개 검사 규칙·도입 문의) |
| eula.html | 사용권 계약 (초안) |
| notices.html | 오픈소스 고지 |
| img/ | 화면 이미지 3장 |
| robots.txt / sitemap.xml / .nojekyll | 검색 노출용 |

제품 파일(MoldCheck_dist.html), 고객 도면, 검증현황 문서는 **들어 있지 않습니다.**

## 배포 순서 (10분)

1. **GitHub 에 빈 저장소 생성**
   https://github.com/new → Repository name `moldcheck-site` → **Public** → Create
   (README·gitignore 체크박스는 모두 해제)

2. **`MoldCheck_site` 폴더 안의 `MoldCheck_site_deploy.bat` 더블클릭**
   로그인 창이 뜨면 GitHub 계정으로 로그인.

3. **Pages 켜기** (스크립트가 자동으로 이 화면을 열어줍니다)
   Settings → Pages → Source `Deploy from a branch` → Branch `main` / `(root)` → Save

4. 1~2분 뒤 아래 주소가 열립니다.
   **https://leejaehaeng.github.io/moldcheck-site/**

## 이후 수정할 때
파일만 고치고 `MoldCheck_site_deploy.bat` 다시 실행하면 사이트가 갱신됩니다.

## 주소를 바꾸고 싶다면
회사 도메인(예: `moldcheck.co.kr`)이 있으면
Settings → Pages → Custom domain 에 입력하고, 도메인 업체 DNS 에
`CNAME  →  leejaehaeng.github.io` 를 추가하면 됩니다.

---

# 판매 시작 전에 반드시 확인해야 할 것

사이트는 지금 올려도 되지만, **돈을 받기 전에** 아래 3가지는 제가 대신 해드릴 수 없는 항목입니다.

### 1. EULA 법률 검토 (필수)
`eula.html` 은 **변호사 검토를 받지 않은 초안**입니다.
특히 제6조(보증의 제한)·제7조(책임의 제한) — 검사 결과를 믿고 가공했다가
불량이 났을 때 책임 범위를 정하는 조항입니다. 이 상태로 유료 계약을 맺는 것은 위험합니다.
→ 유료 판매 전 변호사 1회 검토 권장. 그 전까지는 **무료 파일럿만** 진행.

### 2. NX 저널 실기 검증 (미완료)
MoldCheck 가 만든 NX 저널을 **실제 NX 에서 재생해 본 적이 없습니다.**
사이트에는 "NX 작업 흐름 그대로" 라고 적혀 있습니다.
→ 고객에게 보여주기 전에 사내 NX 에서 저널 1건을 직접 돌려보고 결과를 확인하세요.

### 3. 기존 MoldCheck 저장소 공개 여부
https://github.com/LeeJaeHaeng/MoldCheck 가 **Public 이면 지금 즉시 Private 으로 바꾸세요.**
그 저장소에는 `docs/검증현황.md`(고객 도면 파일명 HA/JW/MDT, 7889/7891 포함)와
아직 검증되지 않은 항목 목록이 들어 있습니다.
→ Settings → 맨 아래 Danger Zone → Change repository visibility → Private

이번에 만드는 `moldcheck-site` 는 위 저장소와 **별개의 새 저장소**이므로,
사이트를 Public 으로 올려도 내부 문서는 노출되지 않습니다.

---

# 사이트에 적힌 숫자의 근거

| 표시 | 근거 |
|---|---|
| 재현율 100% / 정밀도 100% | 정답을 아는 검증용 모델 3종·홀 52개 대조 (`tests/mk_bench.py`) |
| 홀 종류 1순위 88.5% | 같은 52개 홀, 지름이 여러 규격과 겹치는 경우는 두 해석 병기 |
| 판 역할 100% | 금형 4종·부품 40개 (`tests/mk_mold.py`) |

이 수치는 **검증용 모델 기준**이며 사이트에도 그렇게 명시해 두었습니다.
실제 고객 도면에서의 정확도는 파일럿에서 확인해야 합니다. 과장 표기는 넣지 않았습니다.
